import 'package:flutter/material.dart';

class GoodToKnowPage extends StatelessWidget {
  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text('Good to know Page')
      ),
    );
  }
}